---
- name: converge
  hosts: molecule-takel-nginx  # all
  gather_facts: false
  roles:
    - takel_ssl
    - takel_nginx

- name: converge
  hosts: molecule-takel-nginx-system
  gather_facts: false
  tasks:
    - name: Install curl
      apt:
        name: curl
        update_cache: true
